# 更新说明

## 2026-06-15：Skill 加固、审查与测试补强

本次更新根据 Claude 对 `a-share-etf-pcf-metrics` skill 的评审意见，对代码、文档、可复现性和测试覆盖做了系统加固。

### 修复的问题

- 修复 `scripts/run_a_share_dividend_etf_pcf_metrics.py` 中 `load_a_stock_snapshot()` 递归调用自身的问题，避免 A 股快照加载路径触发无限递归。
- 修复 `scripts/pcf_lookthrough.py` 中 `clean_percent()` 对深交所 `PremiumRatio` 的解析风险：当接口返回 `10` 表示 10% 时，现在会转换为 `0.10`。
- 将 `run_pcf_metrics.py` 默认请求间隔从 `0` 调整为 `0.05`，降低批量估值查询时被限流的概率。
- 默认输入缺失时，错误提示会说明 `lookthrough-hk-all-ranking/all_etf_summary.csv` 是前置流程产物，并提示可直接使用 `--etf`。

### 结构改进

- 新增 `scripts/pcf_common.py`，集中管理核心计算口径：
  - 加权平均
  - 盈利收益率聚合 PE
  - 年化收益
  - ETF 价格/净值表规范化
- A 股、港股、自选 ETF 组合脚本保留原函数名，但内部改为调用公共函数，降低口径漂移风险。

### 文档改进

- 重写 `SKILL.md`，移除本机绝对路径，改为 `$SKILL_DIR` 和相对路径。
- 重写 `README.md` 与 `README中文.md`，明确两个工作流：
  - 批量港股 ETF PCF 指标表
  - 自选 ETF / ETF 组合穿透
- 明确 `lookthrough-hk-all-ranking/all_etf_summary.csv` 的前置来源，不再假设该文件天然存在。
- 明确 `run_pcf_metrics.py` 默认会删除中间文件，调试和审计时应使用 `--keep-intermediates`。
- 更新 `agents/openai.yaml`，修复原先的乱码展示。
- 更新 `references/pcf-method.md`，补充交易日/T-1 数据意识和来源日期保留要求。

### 新增文档

- `references/output-schema.md`
  - 记录最终 CSV/XLSX 和自选 ETF 输出表的字段含义、单位和空值语义。
- `references/troubleshooting.md`
  - 记录常见失败场景和处理方式，包括默认输入缺失、PCF 下载失败、估值缺失、收益指标为空、权重合计不等于 100 等。
- `requirements.txt`
  - 记录运行依赖：`akshare`、`pandas`、`requests`、`openpyxl`、`tabulate`。

### 测试补强

新增 `tests/test_core_behaviour.py`，覆盖以下行为：

- `load_a_stock_snapshot()` 不再递归。
- `clean_percent()` 正确处理 `10%`、`10` 和 `0.1`。
- PE 使用盈利收益率聚合口径。
- ETF 组合估值按底层股票权重聚合。
- 默认输入缺失时给出可执行错误提示。
- `clean_intermediates()` 只保留最终 CSV/XLSX。
- `clean_float()` 拒绝 `inf`、`-inf`、`nan`。
- `is_a_stock_code()` 拒绝短码补零造成的误判。

### 验证结果

已完成以下验证：

```powershell
python -m py_compile scripts\pcf_common.py scripts\run_pcf_metrics.py scripts\pcf_lookthrough.py scripts\run_a_share_dividend_etf_pcf_metrics.py scripts\selected_etf_lookthrough.py tests\test_core_behaviour.py
python -m unittest discover -s tests -v
python scripts\selected_etf_lookthrough.py --help
python scripts\run_pcf_metrics.py --help
python scripts\pcf_lookthrough.py --help
python scripts\run_pcf_metrics.py --etf 159569 --out-dir pcf-metrics-smoke-test --keep-intermediates --top-n 1 --sleep 0.01
```

验证结论：

- 8 个离线单元测试全部通过。
- 批量主入口联网 smoke test 通过。
- 生成的 `pcf_full_metrics_table.csv` 和 `pcf_full_metrics_table.xlsx` 可正常读取。
- 搜索确认没有旧的本机绝对路径、递归调用或旧错误提示残留。
- 工作区临时输出已清理，`.gitignore` 已覆盖常见生成文件。

### GitHub 提交

本次加固已推送到 `main`：

- `49e4bde`：`Harden ETF PCF metrics skill`
- `27ffc18`：`Add data validation regression tests`

## 2026-05-25：新增自选 ETF / ETF 组合穿透脚本

新增 `scripts/selected_etf_lookthrough.py`，用于对用户指定的 A 股上市 ETF 或 ETF 组合做 PCF 底层穿透，不依赖模型能力即可生成结果表。

主要能力：

- 支持单只 ETF 穿透，也支持 ETF 组合穿透。
- 支持 A 股底层资产和港股底层资产。
- 自动读取交易所 PCF，优先用完整申赎清单而不是前十大持仓。
- 给底层股票补充 PE、PB、股息率等估值和分红指标。
- 汇总 ETF 级指标：股息率、PE、PB、年化收益、Sortino Ratio、波动率、近半年收益、近一年收益、近 3 年收益。
- 汇总组合级指标：按穿透后的底层股票权重重新计算股息率、PE、PB。
- 组合的年化收益、波动率和 Sortino Ratio 使用 ETF 历史净值或价格合成组合曲线后计算，不使用 ETF 表格指标的简单加权。

新增输出：

- `metrics_summary.csv`
- `lookthrough_summary.csv`
- `lookthrough_detail.csv`
- `etf_summary.csv`
- `lookthrough_report.xlsx`

示例：

```powershell
python scripts\selected_etf_lookthrough.py --etf 159569,159758 --weights 60,40 --out-dir selected-etf-output
```

对应提交：

- `8aaf2f8`：`Add selected ETF portfolio lookthrough`

