# 更新说明

更新时间：2026-05-25

## 本次更新

这次新增了一个纯脚本流程：`scripts/selected_etf_lookthrough.py`。

它用于对用户指定的 A 股上市 ETF 或 ETF 组合做 PCF 底层穿透，不依赖模型能力即可生成结果表。

主要能力：

- 支持单只 ETF 穿透，也支持 ETF 组合穿透。
- 支持 A 股底层资产和港股底层资产。
- 自动读取交易所 PCF，优先用完整申赎清单而不是前十大持仓。
- 给底层股票补充 PE、PB、股息率等估值和分红指标。
- 汇总 ETF 级指标：股息率、PE、PB、年化收益、Sortino Ratio、波动率、近半年收益、近一年收益、近 3 年收益。
- 汇总组合级指标：按穿透后的底层股票权重重新计算股息率、PE、PB。
- 组合的年化收益、波动率和 Sortino Ratio 使用 ETF 历史净值或价格合成组合曲线后计算，不用表格中的 ETF 指标做简单加权。

## 新增输出

运行 `selected_etf_lookthrough.py` 后会生成：

- `metrics_summary.csv`：每只 ETF 一行，另有一行 `PORTFOLIO` 组合指标。
- `lookthrough_summary.csv`：组合穿透后的底层股票汇总。
- `lookthrough_detail.csv`：每只 ETF 的底层明细，并包含股票 PE、PB、股息率。
- `etf_summary.csv`：ETF PCF 来源、持仓期和指标汇总。
- `lookthrough_report.xlsx`：包含以上表格的 Excel 工作簿。

## 示例

```powershell
python scripts\selected_etf_lookthrough.py --etf 159569,159758 --weights 60,40 --out-dir selected-etf-output
```

参数说明：

- `--etf`：ETF 代码，多个代码用逗号或空格分隔。
- `--weights`：组合权重，可写百分比或小数；不传时默认等权。
- `--markets`：可选，指定 `auto`、`hk` 或 `a`；默认自动识别。
- `--skip-metrics`：只导出穿透权重，不抓取 PE/PB/股息率和收益风险指标。
- `--metrics-workers`：A 股股票估值查询并发数。
- `--nav-lookback-days`：ETF 历史净值或价格回看天数。

## 验证

已用以下命令做过 smoke test：

```powershell
python -m py_compile selected_etf_lookthrough.py
python selected_etf_lookthrough.py --etf 159569,159758 --weights 60,40 --out-dir selected_etf_lookthrough_smoke_metrics --metrics-workers 4 --hk-sleep 0.02
```

测试结果：

- 生成 79 条底层持仓。
- `metrics_summary.csv` 生成 3 行：2 只 ETF 和 1 行组合。
- `lookthrough_report.xlsx` 可正常读取，包含 `穿透汇总`、`穿透明细`、`ETF汇总`、`指标汇总` 四个 sheet。

