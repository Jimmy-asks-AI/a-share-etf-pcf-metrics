interface:
  display_name: "A-share ETF PCF Metrics"
  short_description: "Generate PCF look-through valuation, dividend, and risk metrics for A-share listed ETFs."
  default_prompt: "Use $a-share-etf-pcf-metrics to generate ETF PCF look-through metrics for the ETFs I provide."
policy:
  allow_implicit_invocation: true
